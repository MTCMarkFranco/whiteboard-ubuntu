# System Dependencies (installed via apt-get)
# sudo apt-get install python3-gi python3-gi-cairo gir1.2-gtk-3.0 gir1.2-webkit2-4.0

# Python dependencies (already available through system packages)
# PyGObject is provided by python3-gi system package
# GTK and WebKit2GTK bindings are provided by gir1.2-* packages

# No additional pip packages required - all dependencies are system packages
